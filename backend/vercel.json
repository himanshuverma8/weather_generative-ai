{
  "version": 2,
  "builds": [
    {
      "src": "api/**/*.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    }
  ],
  "env": {
    "OPENWEATHER_API_KEY": "@openweather_api_key",
    "GEMINI_API_KEY": "@gemini_api_key"
  }
}

